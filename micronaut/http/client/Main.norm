package micronaut.http.client

import micronaut.http.client.annotation.Client

@Client(value: "http://localhost")
interface LocalClient {
}

Void main() {
  if LocalClient.class.annotation<Client>() != null {
    printLine("Micronaut HTTP client")
  }
}
